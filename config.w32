// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mysqlstr", "for mysqlstr support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mysqlstr", "enable mysqlstr support", "no");

if (PHP_MYSQLSTR != "no") {
	EXTENSION("mysqlstr", "mysqlstr.c");
}

